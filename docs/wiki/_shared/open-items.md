---
title: Open items — gaps y deuda técnica cross-service
status: draft
last-verified: 2026-08-09
owners: [_shared]
related:
  - "[[architecture]]"
  - "[[project-roadmap-2026]]"
  - "[[properties-service]]"
  - "[[properties-service-search]]"
  - "[[users-service]]"
  - "[[users-service-user]]"
  - "[[catalog-service]]"
  - "[[avm-training]]"
  - "[[adr-estimated-price-dual-signal]]"
  - "[[adr-gmaps-places-geocoding]]"
  - "[[adr-bulk-idempotent-external-id]]"
  - "[[adr-admin-offset-pagination]]"
  - "[[adr-verification-reversible-lifecycle]]"
  - "[[adr-moderation-panel-staged-form]]"
  - "[[adr-transitions-served-by-backend]]"
  - "[[adr-promotions-own-subtab]]"
  - "[[properties-service-admin]]"
  - "[[properties-service-bulk-create-worker]]"
  - "[[properties-service-users]]"
  - "[[frontend-admin-panel]]"
  - "[[frontend-architecture]]"
  - "[[adr-vue-cli-deferred-vite-migration]]"
  - "[[adr-firebase-removal]]"
sources: [../../sources/properties-service/2026-05-28-foundational-exploration.md, ../../sources/users-service/2026-05-28-foundational-exploration.md, ../../sources/frontend/2026-06-04-feed-filters-contract.md, ../../sources/_shared/2026-06-09-mvp-audit-scores.md, ../../sources/catalog-service/2026-06-23-overpass-406-and-h3-chicken-egg-fix.md, ../../sources/users-service/2026-06-23-public-profile-endpoint.md, ../../sources/properties-service/2026-07-16-bulk-create-sync-timeout-risk.md, ../../sources/properties-service/2026-07-16-bulk-create-owner-id-resolution.md, ../../sources/properties-service/2026-07-17-bulk-async-redesign-presigned-retry.md, ../../sources/catalog-service/2026-07-18-h3-precompute-and-bulk-resolve.md, ../../sources/properties-service/2026-07-19-bulk-create-worker-streaming-csv.md, ../../sources/properties-service/2026-07-22-bulk-create-properties-refactor.md, ../../sources/properties-service/2026-07-27-bulk-async-import-worker.md, ../../sources/users-service/2026-07-27-resolve-accounts-by-email.md, ../../sources/properties-service/2026-07-28-bulk-import-smoke-test.md, ../../sources/frontend/2026-07-28-admin-panel-groundwork.md, ../../sources/properties-service/2026-07-29-moderation-state-machines-block-imports.md, ../../sources/frontend/2026-07-29-admin-table-tanstack-and-cleanup.md, ../../sources/properties-service/2026-08-01-bulk-import-pending-verification.md, ../../sources/frontend/2026-08-01-admin-panel-tabs-moderation-preview.md, ../../sources/properties-service/2026-08-02-moderation-lifecycle-verified-not-terminal.md, ../../sources/properties-service/2026-08-09-server-driven-transitions-and-promotable-filter.md, ../../sources/properties-service/2026-08-09-promotions-schema-pagination-and-expiry-filter.md, ../../sources/frontend/2026-08-09-moderation-wiring-and-promotions-shell.md, ../../sources/frontend/2026-08-02-moderation-panel-form-over-buttons.md]
---

## TL;DR

Backlog vivo de gaps detectados al documentar la wiki (2026-05-28): cosas que el código aún no tiene pero la arquitectura asume, contradicciones, y bordes operativos. Marcá `[x]` a medida que se cierren. No es un reemplazo del issue tracker — es la vista cross-service para no perder contexto entre sesiones. Ver [[project-roadmap-2026]] para la visión de alto nivel por fases.

> Convención: cada ítem enlaza a la página/ADR que lo origina. Si cerrás uno, marcá `[x]` en su lugar y movelo a `## Cerrados` — nunca borres el texto. Si aplica, actualizá el claim de la página de origen.

---

## Fase 2 — Properties (front pendiente)

- [ ] **Vista detalle de propiedad (`/properties/:id`) — parcial.** Click en card del feed → página de detalle con galería completa, info del listing, precio estimado AVM, mapa con ubicación. **Update 2026-06-12 — la base ya existe**: `PropertyDetailView.vue` + `usePropertyDetail.ts` en `/listing/:id` y el endpoint `GET /v1/properties/{property_id}` ([properties.py:109](backend/properties-service/src/app/api/routes/properties.py#L109)). Falta: **popup/galería de fotos**. Las isócronas/POIs ya están implementadas (cerrado arriba). Ver [[properties-service-listing]], [[frontend-architecture]].
- [ ] **Form publicar propiedad + gestión de listings del dueño (`/properties`).** Formulario multi-step: datos básicos → ubicación → imágenes (presigned batch). Vista `/properties` lista los listings propios del usuario autenticado (`GET /v1/properties/mine`). Ver [[properties-service-listing]], [[adr-image-upload-presigned-batch]].
- [ ] **Panel de moderación admin.** Vista interna para aprobar/rechazar listings que infringen políticas — state machine `pending → active | rejected`. Solo accesible con rol admin (Keycloak). Ver [[properties-service-admin]].
---

## Fase 5 — Diferenciadores

### Crítico — flujo de valor

- [ ] **Worker de properties que consume `price-predicted`.** Cablear el consumer que escucha el topic de [[analytics-service]] y llama `SetEstimatedPriceUseCase` con `principal=None` para poblar `ml_estimated_price`. Hoy `workers/` de properties solo tiene el bulk-create worker (no hay consumer Kafka) y el path ML no tiene caller — es el flujo async properties↔analytics que [[architecture]] marca "en definición". Sin esto, el precio del AVM nunca llega al listing. Ver [[adr-estimated-price-dual-signal]], [[properties-service-admin]].
- [ ] **Desactivación de cuenta no propaga a los listings del usuario.** `DeactivateCurrentAccountUseCase` (`deactivate_current_account.py:20-42`) marca la cuenta `is_active=False` e invalida su cache, pero no notifica a nadie — los listings del usuario en [[properties-service]] quedan publicados y visibles en el feed aunque el dueño ya no esté activo. Falta: publicar un evento (ej. Kafka `account.deactivated {account_id}`) y un consumer en properties-service que marque `inactive` todos los listings de ese `owner_id`. Mismo patrón fire-and-forget que `price-predicted` (ítem anterior). Ver [[users-service]], [[properties-service-admin]].

### Impresiones, analytics de comportamiento y feed personalizado

- [ ] **Tracking de impresiones por listing.** Registrar cuántas veces fue visto cada listing (impression) y por quién (si está autenticado). Candidato a evento Kafka `listing.impressed` consumido por analytics-ms. Sin esto, el propietario no puede saber el alcance de su publicación. El enfoque (beacon de cliente → collector tonto → Kafka → consumer en analytics) está fijado en [[adr-impressions-beacon-pipeline]]. Ver [[properties-service]], [[analytics-service]].
- [ ] **Modelo de recomendación de promociones.** Con el historial de impresiones se puede entrenar un modelo que identifique qué listings promocionados tienen mayor probabilidad de conversión para cada perfil de usuario. Input: intereses del usuario + historial de views + features del listing. Ver [[analytics-service]], [[adr-estimated-price-dual-signal]].
- [ ] **Feed personalizado por comportamiento.** Hoy el feed filtra por preferencias declaradas (onboarding). Con tracking de comportamiento (qué vio, cuánto tiempo, si volvió) se puede alimentar un recomendador colaborativo o content-based que mejore el ranking. El bbox del mapa estilo Airbnb ya da señal de zona de interés implícita. Ver [[frontend-architecture]], [[analytics-service]].
- [ ] **Targeting de listings promocionados.** Cruzar perfil del usuario (barrios, tipo de propiedad) con historial de comportamiento para mostrar los promoted listings a las personas con mayor probabilidad de conversión — no al azar. Depende de los dos ítems anteriores.

### Producto — diferenciadores y gaps de mercado

- [ ] **Score de oportunidad de inversión inline en el feed.** Calcular `(precio_listado - precio_estimado) / precio_estimado` y exponerlo en `PropertyCardSchema` como campo numérico + badge visual en cada card. Diferenciador directo vs FincaRaíz/Metrocuadrado (sin AVM); supera a Cerouno (calculadora manual vs automático en cada card del feed). Depende del worker AVM → listing (`ml_estimated_price` poblado). Ver [[adr-estimated-price-dual-signal]], [[properties-service-search]].
- [ ] **Alertas de oportunidad de precio.** Notificación push/email cuando se lista una propiedad X% por debajo del precio estimado en la zona de interés del usuario. Alta conversión para perfiles de inversión — nadie en el mercado colombiano lo tiene automático. Requiere: AVM funcionando + sistema de notificaciones + preferencias de zona ya capturadas en onboarding. Ver [[adr-estimated-price-dual-signal]], [[users-service]].
- [x] **`POST /v1/geo-resolution/reachable-pois` en catalog-service (isócronas).** Implementado 2026-06-15. ORS → `polygon_to_cells(r9)` → `get_by_h3_cells` (1 query) → groupby en memoria → `list[ReachablePoisResult]`. Soporta múltiples perfiles en parallel gather. Ver [[catalog-service]], [[catalog-service-ors]], [[adr-isochrone-ors-h3]].
- [ ] **Búsqueda con lenguaje natural.** LLM con tool use que traduce una query en lenguaje natural a la estructura `preferences + filters` del endpoint `/v1/search/feed`. Cerouno ya la tiene → brecha competitiva activa. El endpoint de búsqueda ya soporta todos los parámetros necesarios; falta la capa de traducción. Ver [[properties-service-search]], [[frontend-architecture]].
- [ ] **Estrato socioeconómico en el modelo de propiedad.** Agregar campo `stratum` (1–6) a la tabla de propiedades. En Colombia el estrato predice precio, servicios públicos y entorno — feature de alto valor para el AVM y para filtros del feed. Habi lo expone en cada card; FincaRaíz y Metrocuadrado también lo tienen. Sin él, el AVM pierde una de las variables más predictivas del mercado colombiano. Ver [[properties-service-admin]], [[avm-training]].
- [ ] **Página pública de perfil del publicante.** Mostrar quién publicó una propiedad (no solo el dashboard interno para admin) implica una página de usuario navegable por terceros, no solo un id. Camino mínimo: link "ver más de este publicante" reusando `GET /properties/users/{user_id}` (ya existe, devuelve `list[PropertyCardSchema]`) en una vista de feed filtrada — sin perfil nuevo. Camino completo: perfil público real (avatar, bio, rating, contacto) requiere nuevos campos/UCs en users-service expuestos a terceros (hoy probablemente solo el propio usuario autenticado los puede ver) — mucho más alcance, necesario si el publicante va a ser un actor visible (mensajería, reputación). Decidir alcance antes de implementar. Ver [[properties-service-listing]], [[users-service]].
  - [ ] **Sub-tarea — paginación server-side offset/limit en `GET /v1/properties/users/{user_id}`.** Hoy el endpoint devuelve la lista completa sin paginar (`SqlPropertyRepository.get_public_user_properties`, sin `.offset()/.limit()`). Con datasets de prueba grandes (admin bulk) se nota; un agente real no tendría tantos listings, pero paginar es barato porque `owner_id` ya está indexado y `Property` tiene `created_at` (vía `AuditMixin`) para ordenar de forma estable. Tocar: router (`properties.py`) → use case (`get_public_user_properties.py`) → port (`property_repository.py`) → repo SQL, agregando `limit`/`offset` con defaults. En frontend, `PublicProfileView.vue` pasa de paginación local (`slice` sobre mock) a un composable real que pagina por offset con un cache simple por página (no necesita cursor-stack como `useFeed`, porque el offset es invertible). Pendiente: el dev lo va a implementar manualmente para practicar.

---

## Deuda técnica cross-fase

### Cadena del frontend (Google Maps → predicción)

- [ ] **Refactor `/geo-resolution` en catalog.** Deprecar `resolve-neighborhood` (forward Mapbox, duplica el SDK del front). `by-coordinates` ya tiene `BackgroundTasks` de POIs ✅ y `locality_id` ✅ — solo falta deprecar el otro endpoint. Ver [[catalog-service]], [[adr-mapbox-frontend-only]].
- [ ] **Restricción de HTTP referrer** en la API key de Google Maps antes de producción (en dev corre sin restricción de dominio). Ver [[adr-gmaps-places-geocoding]].

### Deuda geo / ML

- [ ] **Re-registrar AVM con `year_built` nullable.** `_make_raw_input_example()` en `trainer.py` usa `year_built: 2012` (int) → MLflow infiere `long required` → rechaza `null` en runtime antes del preprocessing. Fix: pasar `year_built: None` en el ejemplo y re-correr `final_train` + promover alias `production`. Workaround temporal: reemplazar `None` con `0` en `AVMModelAdapter` tras el `model_dump`. Ver [[analytics-service-mlflow]].
- [x] **Conciliar tag set de POIs.** `category_map.py` unifica 5 keys OSM, 15 categorías, 147 valores — idéntico al tag set del AVM. `extract_category()` reemplaza el mapper anterior. Cerrado 2026-06-11. Ver [[catalog-service-overpass]], [[adr-geospatial-feature-engineering]].
- [ ] **Evaluar self-host de Overpass.** La instancia pública `overpass-api.de` empezó a devolver 406 a requests sin `User-Agent` descriptivo (mitigado 2026-06-23 seteando `settings.OVERPASS_USER_AGENT`) y sigue devolviendo `ServerLoadError` (504) bajo carga — riesgo de disponibilidad fuera de nuestro control. Self-host requiere levantar un Overpass propio (similar al contenedor ORS ya en `infra/`) y subir/mantener actualizados los extractos `.pbf` (Colombia/Bogotá) al servicio. Pendiente decidir si vale el costo operativo vs. seguir absorbiendo los fallos del público con retries. Ver [[catalog-service-overpass]].
- [ ] **Resolución H3 al cablear feature store desde un MS (caveat, no bug).** Los servicios indexan en r9 (lookup espacial granular) y el AVM usa r6/r7/r8 (feature del vector; r9 mete ruido). Hoy NO rompe nada porque el modelo recomputa sus celdas desde `lat/lon` en inferencia y no consume las celdas de los MS. Cuando se conecte el feature store desde un MS al modelo, **recomputar la resolución del modelo, no reusar la celda r9 almacenada**. Decisión en [[adr-h3-resolution-per-use-case]]; documentado en [[glossary#h3]].
- [ ] **CI + promoción del training AVM.** Automatizar el run (orchestrator tipo Airflow) y formalizar la promoción del alias `production` (hoy manual). Ver [[avm-training]], [[adr-model-promotion-external-to-service]].

### properties-service — arquitectura interna

- [ ] **El precio estimado no se puede leer desde ningún cliente.** `admin_estimated_price` y `ml_estimated_price` no aparecen en `PropertyDetailSchema`, `PropertyCardSchema` ni `AdminPropertyCardSchema` — la única mención de `estimated_price` en los schemas es la del *request*. O sea que `POST /admin/properties/{id}/estimated-price` es write-only: el admin escribiría un precio sin ver el guardado ni el que estimó el modelo, pisándolo a ciegas. **Update 2026-08-09**: el detalle admin ya no comparte schema — devuelve `AdminPropertyDetailSchema` — así que el bloqueo se redujo a la key `cache_property` compartida, que sigue impidiendo exponer campos **almacenados** sin publicarlos. Para derivados el problema desapareció (se calculan a la salida, ver [[adr-transitions-served-by-backend]]); para el precio estimado hace falta una key propia para el detalle admin, o guardar el superset y dejar que cada schema descarte lo que no le toca. Por eso el input quedó fuera del panel de moderación. Ver [[properties-service-admin]], [[frontend-admin-panel]], [[adr-estimated-price-dual-signal]].
- [ ] **El detalle admin no tiene test propio, y ahora calcula reglas de negocio.** `GetPropertyDetailAdminUseCase` pasó de devolver el schema público a construir `AdminPropertyDetailSchema` con los destinos legales (`_with_transitions`), o sea que un error ahí hace que el panel ofrezca transiciones equivocadas — el mismo fallo que el cambio venía a eliminar. No hay test que lo cubra: se verificó a mano contra un payload real el 2026-08-09. Falta también cubrir el filtro `is_promoted` del listado, que hoy solo está verificado por el SQL que genera. Ver [[properties-service-admin]], [[adr-transitions-served-by-backend]].
- [ ] **Las promociones no expiran solas: falta el job.** Nada corre cuando una promoción pasa su `ends_at` — `is_active` sigue en `True` hasta que un admin la quita a mano, así que una campaña vencida seguiría sirviéndose como ad pago y contando como "promocionada". **Mitigado el 2026-08-09** filtrando la fecha en cada lectura: `active_promotion_clause()` (`models/promotion.py`) es `is_active AND ends_at > now()` y lo usan las cuatro consultas que definen "vigente" —el listado admin, el filtro `is_promoted`, el join de ads del feed y la relación `Property.promotions` vía `primaryjoin`—. Es una curita: la fila queda `is_active=True` para siempre, invisible para todos los caminos, y como `get_active_by_property_id` también filtra por fecha, una promoción vencida ya no se puede quitar por el DELETE (404). El fix real es un job que las marque `is_active=False` al vencer, y recién ahí el filtro de fecha se vuelve redundante. Ojo con dónde vivirá: properties-service no tiene scheduler hoy — el precedente es el worker APScheduler in-process de users-service ([[users-service-kc-compensation]]). Ver [[properties-service-admin]].
- [ ] **No existe `verified_at`: se sabe quién aprobó, no cuándo.** Desde el 2026-08-02 `VerifyPropertyUseCase` firma `verified_by`, pero el momento de la resolución no se guarda en ningún lado — `updated_at` lo pisa la siguiente escritura, incluida la del dueño editando el precio. Agregarlo es una columna nueva y su migración de Alembic. Ver [[properties-service-admin]], [[adr-verification-reversible-lifecycle]].
- [ ] **Sin historial de moderación.** Con `verified` ya no terminal, una property se puede resolver varias veces (aprobar → revocar → reencolar → aprobar) y solo la última sobrevive en `verified_by`/`rejection_reason`. Responder "¿cuál fue el historial de esta property?" requiere una tabla de eventos de moderación. Se decidió explícitamente **no** construirla ahora. Ver [[adr-verification-reversible-lifecycle]].
- [ ] **El handler de errores descarta `context`, así que el 409 llega sin datos.** `base_error_handler` devuelve solo `{message, code}`; el `{current, target}` que carga `InvalidStatusTransitionError` se loguea pero no viaja al cliente, y el `message` está en inglés. Un front que reciba `409 INVALID_STATUS_TRANSITION` no puede decir "no se puede pasar de X a Y" con datos — solo texto genérico. Afecta a todos los `BaseError` con contexto, no solo a este. Ver [[properties-service-admin]], [[frontend-admin-panel]].

- [ ] **Refactor: `CreatePropertyUseCase` construye el ORM model `Property` directamente (`create_property.py:62`).** El UC debería pasar el schema de dominio al repo y que el repo haga el mapeo al ORM model — leak de hex arch detectado 2026-06-30. Ver [[properties-service-listing]].
- [ ] **Los 1.256 fallos de geo del import de 20k no están triados.** Mezclan tres causas: basura del scrape (`0.0,0.0`), coordenadas fuera de Bogotá (Cartagena, Pasto, Illinois, España) y coordenadas legítimamente bogotanas que ningún polígono de barrio contiene. **Solo la tercera es un gap**; las dos primeras está bien que fallen. Sin separarlas no se puede decir si el 6,3% es aceptable o si falta cobertura de barrios en catalog. Query de diagnóstico: `ST_Contains` sobre un punto conocido de Chicó — si da 0 filas pero el barrio vecino existe, es hueco de teselado de IDECA. Ver [[properties-service-bulk-create-worker]].
- [ ] **El re-import de idempotencia sigue sin hacerse.** Correr el mismo CSV dos veces debería dejar `count(*) FROM properties` sin cambiar, con `updated_at` refrescado y `created_at` intacto. Son 3 minutos y es la validación empírica de [[adr-bulk-idempotent-external-id]], que hoy se sostiene solo por razonamiento y tests unitarios.
- [ ] **El 401 por token malformado falta en catalog, analytics y users.** `get_signing_key_from_jwt` decodifica el token para leer su `kid`, así que uno malformado lanza `DecodeError`, que **no** es un `PyJWKClientError` y se escapa como 500 con stack trace. Arreglado en properties-service el 2026-07-27; catalog-service y analytics-service tienen los mismos dos `except` incompletos, y **users-service está peor**: solo atrapa `PyJWKClientConnectionError`, así que también se le escapan los `PyJWKClientError` (ej. `kid` desconocido).
- [ ] **`inserted` cuenta filas escritas, no properties creadas.** Con semántica de upsert no son lo mismo: en el import de 20k, 18.744 filas escritas incluían duplicados que hicieron update. Si el front lo muestra como "N propiedades creadas" estaría mintiendo. Se planteó renombrarlo a `written` o `processed` y no se decidió. Ver [[properties-service-bulk-create-worker]].
- [ ] **Dedup residual: `PropertyImage` conflictúa por `url`.** `collapse_duplicate_ids` resuelve las colisiones por `property_id` (y de paso las de `PropertyLocation`, que deriva de él), pero dos properties distintas que compartan una URL de imagen dentro del mismo chunk romperían el `bulk_insert` con el mismo `CardinalityViolation` y el mismo síntoma confuso. Hoy está dormido porque `image_urls` va vacío en el seed. Ver [[adr-bulk-idempotent-external-id]].
- [ ] **Sin acción de "deshacer un import", y sin TTL/content-type en el upload.** El redo diseñado el 2026-07-17 (borrar las properties de un batch y reprocesar) nunca se construyó. Ya no depende de una columna: con ids determinísticos el set de properties de un import se deriva del CSV (`uuid5` por `external_id`), que sigue en storage vía `bulk_jobs.storage_key`. Siguen pendientes del diseño original el TTL de retención del archivo en storage y la validación de content-type antes de emitir la presigned URL — y ojo, si se agrega el TTL sin construir antes el redo, se pierde también la vía de derivar el set desde el CSV. Ver [[adr-bulk-idempotent-external-id]], `docs/sources/properties-service/2026-07-17-bulk-async-redesign-presigned-retry.md`.
- [ ] **IMPORTANTE — los CSV de seed no tienen las columnas `external_id` ni `email`.** `seed_bogota_500.csv` y `seed_bogota_5k.csv` traen el header viejo; `BulkPropertyCsvRow` requiere ambas y `StrictBase` es `extra="forbid"`, así que hoy **fallarían el 100% de las filas**. `external_id` es obligatorio para la idempotencia (ver [[adr-bulk-idempotent-external-id]]) y `email` para resolver el `owner_id` real. Detectado 2026-07-27 al conectar el worker end-to-end. Cuidado al generarlos: un `external_id` reusado entre archivos distintos **sobreescribe** la property anterior en vez de crear una nueva. **Update 2026-07-28**: se generó `seed_bogota_20k.csv` desde los archivos de scrape *con* ambas columnas, y es el que se usó para el smoke test. Los dos seeds originales (`seed_bogota_500.csv`, `seed_bogota_5k.csv`) siguen sin ellas y sin usarse.
- [ ] **El tamaño del CSV no se valida en ningún lado.** Al mover el upload a presigned PUT, `validate_properties_bulk_upload` dejó de tener callers; `RequestBulkUploadUrlUseCase` recuperó la validación de **extensión** pero el tamaño no es enforceable en un presigned PUT plano — `max_size_bytes` viaja al cliente como hint. Un límite duro requiere migrar a presigned POST con `content-length-range`. `PropertyFileParser` (`services/admin/helpers/file_parser.py`) quedó como código muerto. Detectado 2026-07-27.
- [ ] **Lookup de owner por email case-sensitive.** El filtro SQL (`Account.email.in_(emails)`) y el `email_cache` del worker comparan strings crudos; `StrictBase` hace `str_strip_whitespace` pero no normaliza a minúsculas. Un CSV con `Raul@Mail.com` contra una cuenta `raul@mail.com` falla como "owner not resolved" sin señal de que la causa fue el casing. Sin decidir si normalizar en escritura, en lectura o ambas. Ver [[properties-service-users]].
- [ ] **`JobStatus` no distingue "corriendo" de "encolado".** El enum tiene solo `pending/completed/failed`: durante todo el import el job se ve igual que uno que no arrancó, y lo único que destraba un job zombie es el chequeo de stale de `GetBulkJobStatusUseCase` (`BULK_JOB_TIMEOUT_SECONDS`, 600s) — necesario porque `BackgroundTasks` muere con el proceso. Falta también `expired`, por lo que no se copió la simetría del flujo de imágenes (que sí persiste `status=expired`). Ver [[properties-service-bulk-create-worker]].

### catalog-service — arquitectura interna

- [ ] **`UpdateCountryUseCase` no invalida la lista pública de países.** `update_country.py:41-48` refresca `cache_key_country(id)` pero nunca borra `cache_key_countries()` — a diferencia de `CreateCountryUseCase`, que sí la invalida al crear (`create_country.py:48`). `GET /countries` público (`get_countries.py:20-42`, TTL `CACHE_TTL_CATALOG_SECONDS`) queda con nombre viejo o estado activo/inactivo desactualizado hasta que expire el TTL. Fix de una línea, mismo patrón que ya usa `CreateCountryUseCase`. Detectado en la auditoría de admin endpoints 2026-07-17 (ver sección de properties-service arriba, mismo concern).
- [ ] **Bulk create/enrich de neighborhoods son síncronos end-to-end, mismo problema que properties-service.** `POST /localities/{id}/neighborhoods/bulk` (`BulkCreateNeighborhoodsUseCase`) y `POST /localities/{id}/neighborhoods/bulk/geometry` (`BulkEnrichNeighborhoodGeometriesUseCase`) leen y parsean el archivo completo dentro del request (`admin.py`, hasta 10MB/50MB) y corren todo el procesamiento sync antes de responder. No tenía item propio en este backlog — solo el de properties-service estaba loggeado (07-16). A diferencia de properties, acá el dedup **ya está resuelto**: `bulk_insert` hace `ON CONFLICT (locality_id, code) DO UPDATE` (`sql_neighborhood_repository.py`) — reintentar con el mismo archivo ya es seguro/idempotente hoy. Solo falta la migración a `bulk_jobs` + `BackgroundTasks`, reusando el mismo diseño de tabla que properties-service (`job_type` enum compartido para los dos endpoints de este servicio) — sin necesitar el resto del diseño de redo/dedup de properties-service. Ver ítem de properties-service arriba, `docs/sources/properties-service/2026-07-17-bulk-async-redesign-presigned-retry.md`.
- [ ] **IMPORTANTE — falta un endpoint bulk de geo-resolución; el bulk create de properties-service hace N llamadas individuales.** `BulkCreatePropertiesUseCase._enrich_location` (properties-service) llama `GET /v1/geo-resolution/by-coordinates` **una vez por fila** del CSV — el `Semaphore(50)` acota la concurrencia pero no el total de llamadas: un CSV de 5000 filas son 5000 requests HTTP a catalog-service, cada uno disparando su propia query PostGIS (`ST_Contains`) por separado. Agravante: `CatalogClient.get_location_info()` abre un `httpx.AsyncClient()` nuevo por llamada, sin reuso de conexión. Detectado 2026-07-16, directamente acoplado al refactor async de `bulk_create_properties` (ver ítems de properties-service arriba) — con ese refactor el timeout del cliente deja de ser el síntoma visible, pero la carga real sobre catalog-service no desaparece, solo se vuelve invisible (background). **Update 2026-07-18 — lado catalog-service implementado**: `POST /v1/geo-resolution/by-coordinates/bulk` existe (`BulkResolveLocationsByCoordinatesUseCase` + `get_location_by_points`, resuelve el batch con `unnest(...)` + `LEFT JOIN LATERAL` — una sola query PostGIS en vez de N, con fallback de segunda pasada solo para los puntos que no matchean por el prefiltro h3). **Update 2026-07-19 — la otra mitad ya se conectó, parcialmente**: `CatalogClient.get_locations_bulk` + `_process_batch` (properties-service) ya llaman al endpoint bulk una vez por batch de 2500 filas, en vez de N llamadas individuales. Pendiente: `_process_batch` todavía no está conectado al resto del pipeline de inserción (ver ítem de arriba) — el ahorro de N llamadas ya existe en el código, pero el flujo end-to-end no inserta properties todavía. Ver [[properties-service-catalog]], [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-19-bulk-create-worker-streaming-csv.md`.

### users-service — arquitectura interna

- [ ] **IMPORTANTE — `POST /v1/users/resolve` no tiene ninguna autenticación: es un oráculo de enumeración de emails.** Ni la ruta ni el router `/users` (`APIRouter(prefix="/users", tags=["users"])`, sin `dependencies=`) declaran deps de auth, así que cualquiera con acceso de red puede postear una lista de emails arbitrarios y recibir cuáles tienen **cuenta activa**, junto con su `account_id`. El endpoint existía desde el commit `eda7114`, pero el riesgo cambió de naturaleza el 2026-07-27 al invertir la dirección: antes había que *conocer* un `account_id` (UUID no adivinable) para obtener un email; ahora se prueban emails arbitrarios — pasó de "resolver ids que ya tenés" a "averiguar si `fulano@gmail.com` está registrado". Agravantes: (a) devuelve el `account_id` real, que es la key del perfil público `GET /v1/users/profiles/{account_id}`, así que enumerar emails encadena a leer perfiles; (b) acepta listas, o sea que es enumeración en batch, no de a uno; (c) el filtro `is_active` hace que una cuenta desactivada sea indistinguible de una inexistente, lo cual es bueno para privacidad pero no mitiga la enumeración. Opciones a evaluar: auth servicio-a-servicio (shared secret / mTLS / JWT de cliente), restringirlo a red interna y no exponerlo por el gateway, o rate limiting. Detectado 2026-07-27 al documentar el endpoint, no estaba en la fuente. Ver [[users-service-user]], [[properties-service-users]].

### Infra compartida entre servicios

- [ ] **Centralizar los clientes de Redis y MinIO en una librería interna compartida (single source of truth).** Hoy cada MS copia su propio stack: el de cache Redis (`integrations/cache/redis/cache.py` + `services/shared/adapters/redis_cache_adapter.py` + `services/shared/ports/cache.py` + `core/exceptions/cache.py`) existe por triplicado en catalog, properties y users; el de MinIO (`integrations/storage/minio/storage.py` + adapter + port + exceptions) por duplicado en properties y users. Las copias **ya divergieron** (verificado 2026-06-12, ningún hash coincide): el port de cache de users tiene los 7 métodos base, catalog agrega `set_nx`, properties agrega `mget`/`mget_json`/`mset`/`mset_json` y `delete` multi-key; en storage divergieron hasta funcionalmente — properties expone presigned PUT URLs y users hace `upload_file` server-side. Un bugfix o mejora en una copia no llega a las demás. Propuesta: paquete interno del monorepo (p. ej. `backend/_lib/`, instalable por path con uv) que centralice el cliente Redis (superset de métodos), el cliente MinIO y las excepciones base; cada servicio conserva su port de dominio — el hexagonal se respeta porque lo compartido es el adapter/cliente de infra, no el contrato del dominio. Trade-off: acoplamiento por shared lib (un cambio breaking obliga a actualizar N servicios en lockstep) vs el drift actual de 3+2 copias. **Decisión mapeada en [[adr-shared-infra-lib]] (2026-06-12)**: uv workspace, ports se quedan en cada servicio, scope infra-only; el coste principal es el ajuste de Dockerfiles/build context. Ver [[architecture]], [[adr-cache-optional-layer]]. **Extensión (2026-06-20)**: el mismo problema aplica a las *cache key conventions*, no solo al cliente. `DeletePropertyUseCase`/`UpdatePropertyUseCase` en properties-service necesitan invalidar `geo:reachable:property:{property_id}` (key de catalog-service, ver `get_isochrone_key`) al borrar/mover una propiedad — viable sin llamada cross-service porque Redis es una instancia única compartida (mismo `REDIS_URL` en `.env`), pero hoy properties-service tendría que hardcodear el formato de una key que no le pertenece. Si el paquete compartido centraliza también los builders de cache key (no solo el cliente), este caso se resuelve gratis.

- [ ] **Validar redundancia del `try/except Exception: pass` de cache-aside en los tres servicios.** El ADR [[adr-cache-optional-layer]] documenta ese wrapper en los use cases como el patrón correcto para degradar a DB si Redis falla. En catalog-service se verificó (2026-06-20) que es redundante: `CacheClient.get_json`/`set_json` (`integrations/cache/redis/cache.py`) ya atrapan la excepción internamente y devuelven `None`/`False`, por lo que el wrapper en `ResolveNeighborhoodUseCase` y `ResolvePoiUseCase` no hacía nada — se quitó en ambos. Falta confirmar si las copias de `CacheClient` en properties-service y users-service (ya divergidas, ítem anterior) también atrapan internamente o si ahí el wrapper en el use case sigue siendo necesario porque el cliente propaga la excepción. Si las tres copias atrapan igual, el ADR debería aclarar que el wrapper en el use case es opcional/redundante, no parte del contrato. **Instancias concretas en properties-service pendientes de este veredicto** (auditoría de admin endpoints 2026-07-17): `moderation/set_status.py:54-64`, `moderation/verify.py:50-57`, `promotions/create.py:67-77`, `promotions/delete.py:40-50` — las cuatro comparten el mismo shape de `try/except Exception: pass` alrededor de la invalidación. Resolver la pregunta de redundancia primero (15 min de investigación) y aplicar el mismo veredicto a las cuatro de una — no una por una. Ver [[adr-cache-optional-layer]], [[catalog-service]].
- [x] **Cache-aside para `reachable-pois` con check pre-ORS.** Implementado 2026-06-20. `ResolveIsochroneUseCase.execute()` chequea `cache.get_json` con `get_isochrone_key(property_id=...)` si viene `property_id`, o con el h3 cell del punto (`h3.latlng_to_cell(lat, lon, settings.H3_RESOLUTION)`, no los cells del polígono de la isocrona) si no, antes de llamar `gateway.get_isochrones`. Es todo-o-nada por request porque ORS siempre devuelve los 3 profiles fijos en una sola llamada (`asyncio.gather` en `ors/routing.py`) — no hay cache parcial por perfil. Cachea la response completa (isocrona + POIs) solo si ningún profile dio error, TTL `settings.CACHE_TTL_ISOCHRONE_SECONDS` (1h). Pendiente: invalidación cruzada cuando se actualiza una propiedad (sin reverse index) — ver ítem `geo:reachable:property:{property_id}` arriba. Ver [[catalog-service-ors]], [[adr-isochrone-ors-h3]].

### properties-service — seguridad del feed

- [ ] **Rate limiting en `/search/feed`.** Sin esto el corte de `FEED_MAX_RESULTS` es trivial — N sesiones paralelas cada una llega a 300 orgánicos. Implementar límite por IP y/o por usuario a nivel de API gateway (nginx, Traefik) o middleware FastAPI + Redis. Ver [[properties-service-search]], [[adr-feed-opaque-cursor]].
- [ ] **Cursor firmado (HMAC).** El cursor actual es opaco pero no autenticado: alguien puede decodear el base64, manipular `created_at` para saltar a cualquier punto del dataset, y re-encodear. Un HMAC con secret server-side previene la manipulación sin cambiar el contrato público. Fix de mayor impacto/menor coste si el catálogo tiene valor real. Ver [[adr-feed-opaque-cursor]].
- [ ] **TTL del cursor.** Un cursor válido hoy lo es para siempre. Embeber una expiración (ej. 24 h) en el payload del cursor y validarla en `decode_cursor` fuerza re-inicio del flujo de paginación y reduce la ventana de scraping sostenido. Ver [[adr-feed-opaque-cursor]].

### properties-service — consistencia de cache en admin writes

Auditoría de endpoints admin 2026-07-17 (motivada por la revisión de bulk create) — se revisaron todos los routers admin de los 4 servicios. `users-service` y `analytics-service` no tienen endpoints admin, nada que auditar ahí. `require_admin` está bien implementado en catalog y properties, sin huecos de autorización. Aparecieron los siguientes bugs de cache, agrupados porque son el mismo concern y candidatos a una sola rama chica (`fix/admin-cache-consistency`).

- [ ] **`SetEstimatedPriceUseCase` no invalida cache tras el write.** `estimated_price/set_estimated_price.py:18-38` — a diferencia de todo el resto de writes admin en este router, no hace ningún `cache.delete` tras comitear `admin_estimated_price`/`ml_estimated_price`. Hoy sin impacto visible (esos campos no están expuestos en `PropertyDetailSchema`/`PropertyCardSchema` todavía), pero va a empezar a servir precios stale el día que se expongan. Nota aparte: la rama `principal=None` (líneas 26-32) es código muerto hoy — el único caller es el router admin, que siempre inyecta un `Principal` requerido vía `require_admin`; confirmar si hay un caller interno/ML planeado antes de asumir que ese path está wireado y autorizado.

- [ ] **El worker de import bulk escribe miles de properties y no invalida ninguna cache.** `bulk_create_properties_worker.py` persiste por chunks con el upsert de `bulk_insert` y no toca Redis en ningún punto —los `email_cache` que aparecen ahí son un dict local para resolver dueños, no la cache compartida—, mientras que cualquier write equivalente por la vía normal sí borra sus keys: `set_status` y `verify` ([set_status.py:62-72](backend/properties-service/src/app/services/admin/use_cases/moderation/set_status.py#L62-L72)) invalidan `cache_property`, `client_properties`, `feed_ads_global`/`feed_ads_by_city`, las celdas `map_h3_cell` de r9/r7 y el patrón de `public_user_properties`. En filas nuevas el daño es acotado: feed (`feed:page:*`) y mapa (`map:h3:*`) tienen TTL de 300s, así que el inventario recién importado tarda hasta 5 minutos en aparecer. El caso feo es el reimport, que es precisamente lo que la idempotencia del `external_id` habilita: un CSV corregido hace UPDATE sobre filas que ya existen, y ahí `properties:detail:{id}` vive 6 horas (`CACHE_TTL_PROPERTY_SECONDS`) y `properties:user:{owner}` 30 minutos, sirviendo el dato viejo mientras la BD ya tiene el nuevo. Corregir un precio por CSV y no verlo cambiar en toda la tarde es un bug difícil de atribuir. Opciones: que el worker acumule los ids tocados por chunk y haga un `delete` batch al cerrar el job, más los patrones de feed/mapa una sola vez; borrar 18k keys de detalle una por una no es viable. Detectado 2026-08-24 revisando si el listado de jobs necesitaba cache (no la necesita: ver [[adr-admin-offset-pagination]] y el `list_all` de promociones, que documenta por qué las vistas internas no cachean). Ver [[properties-service-bulk-create-worker]], [[properties-service-admin]], [[properties-service-search]].

### properties-service — deuda pequeña

- [ ] **`PropertyImage.id` es un UUID random sin relación con el uuid de su storage key.** El flujo presigned genera dos UUIDs independientes por imagen: uno para la key (`{property_id}/{uuid}`, `request_presigned_urls.py:49`) y otro para el `id` del row en confirm (`default_factory=uuid.uuid4` del modelo, `confirm_image_uploads.py:88-95` no reusa el de la key). No es un bug — el flujo funciona — pero confunde: para ir del `id` de una fila a su objeto en storage hay que parsear el string `url`, no hay relación directa. A diferencia de POIs (`external_id`+`source` separados del `id` interno, correcto ahí porque ese id lo asigna OSM, un sistema externo), acá los dos UUIDs los genera el propio servicio en dos pasos del mismo flujo — no hay una segunda identidad externa que preservar. Propuesta: que el `uuid.uuid4()` generado al pedir la key sea el mismo que termina como `PropertyImage.id`, en vez de generar uno nuevo en confirm. Bonus: `url` (`f"{base_url}/{bucket}/{property_id}/{id}"`) pasaría a ser derivable en vez de necesitar guardarse — evita filas con `url` stale si `base_url`/bucket cambian (ej. migración de CDN). Detectado 2026-07-17 en discusión de diseño. Ver `request_presigned_urls.py`, `confirm_image_uploads.py`, [[adr-image-upload-presigned-batch]].

- [ ] **Las 18.744 filas importadas antes del 2026-08-01 siguen en `unverified` y nada las va a mover.** El import ahora escribe `pending`, pero solo para filas nuevas: las del smoke test de 20k quedaron en `unverified`, cuyo único salto legal lo dispara un dueño que en un import no existe. Son dos poblaciones conviviendo con reglas distintas — una encolada, otra invisible para siempre. Se necesita un `UPDATE ... SET verification_status = 'pending' WHERE verification_status = 'unverified'`, como script o migración de datos; decidir cuál. Detectado 2026-08-01 al hacer el cambio del worker. Ver [[properties-service-bulk-create-worker]], [[properties-service-admin]].
- [ ] **Una cola de 18.744 en `pending` no la trabaja nadie, y sin criterio de priorización el cambio no resuelve nada.** Ahora que el import encola, la cola existe pero nace impracticable: un admin la abre, ve el número y la cierra. La pregunta abierta es si la verificación es exhaustiva o por muestreo priorizado — precio fuera de rango contra el AVM, propiedades sin fotos, dueños con muchas publicaciones. Cualquiera de esas señales daría una cola de decenas en vez de miles, y todas usan datos que el sistema ya tiene. Sin esto, la state machine queda correcta y decorativa. Ver [[properties-service-admin]], [[business-model]].
- [ ] **El feed no puede avisar que un listing está en revisión.** La posición tomada es publicar avisando —`status` y `verification_status` son ejes independientes, un listing puede estar visible mientras se revisa— pero `PropertyCardSchema` no lleva `verification_status`, así que ni el feed ni el feed-mapa pueden mostrarlo. El detalle público sí (`usePropertyDetail` mapea `pending → "En revisión"`), o sea que el aviso aparece recién después de clickear, y no donde la gente navega. Falta el campo en el schema y el badge en la card. Ojo con el matiz: mientras el 100% del inventario esté en `pending` el badge no comunica nada — recién vale cuando una porción real esté `verified`. Detectado 2026-08-01. Ver [[properties-service-search]], [[properties-service-admin]].

- [ ] **`GET /admin/promotions` no acepta ningún filtro, así que la tab de promociones activas no se puede acotar.** `GetPromotionsAdminRequest` solo lleva `page` y `page_size` ([admin_schemas.py:95](backend/properties-service/src/app/services/admin/schemas/admin_schemas.py#L95)), y el listado sale paginado sin más criterio: con campañas reales encima, el admin no puede aislar las que vencen esta semana ni las de prioridad alta, que son las dos únicas cosas que una promoción decide. Falta sumar los campos al request y propagarlos a `get_all`/`count_all` del repo, como ya hace `GetPropertiesAdminRequest`; recién ahí `AdminFilterBar` —ya montada en moderación contra query params— tiene qué mostrar en esa vista. Caso aparte, la tab de crear sí podría montarla hoy, pero solo con `verification_status`: sus `status: active` e `is_promoted: false` no son filtros de usuario sino las dos reglas que aplica `CreatePromotionUseCase`, y un select que las pise ofrecería propiedades que el backend va a rechazar al promocionar. Detectado 2026-08-24 al cablear los filtros de moderación. Ver [[properties-service-admin]], [[frontend-admin-panel]], [[adr-promotions-own-subtab]].

### frontend — deuda pequeña

- [ ] **El contrato front↔back se escribe a mano en los dos lados, y hoy rompió el front dos veces en runtime.** El backend emite OpenAPI, pero los tipos de `types/admin.ts` y los paths de `constants/propertiesEndpoints.ts` se mantienen a mano. El 2026-08-09 eso falló dos veces en el mismo día: las props `allowed_*` llegaron `undefined` contra un backend que todavía no las servía —el `.map` reventaba el panel entero, mitigado con defaults— y el listado de promociones cambió de array a página, rompiendo la vista hasta adaptarla. Ninguna de las dos las detecta el build: el front compila igual porque los tipos dicen lo que uno escribió, no lo que el servidor responde. Generar los tipos desde el OpenAPI de cada servicio elimina la clase entera; el costo es sumar un paso de generación al flujo de build. Ver [[frontend-architecture]], [[frontend-admin-panel]].

- [ ] **IMPORTANTE — el typecheck del frontend nunca corre: `vue-tsc` aborta por el `tsconfig`.** `tsconfig.json` declara `baseUrl`, deprecado en TS 5.x, y eso emite `TS5101` **antes** de typecheckear nada — o sea que toda lectura de "el typecheck pasa" en este proyecto fue una corrida que no arrancó. Corriéndolo con `--ignoreDeprecations 6.0` aparecen 2 errores reales, pre-existentes y hoy invisibles: `HouseCard.vue:81` importa un default export que `types/properties.ts` no tiene, y `ResetPasswordView.vue:51` usa un `loginUser` inexistente en ese componente. Ninguno rompe el build porque `vue-cli-service build` valida con ESLint y **no** invoca `vue-tsc`. Fix: agregar `"ignoreDeprecations": "6.0"` o migrar `baseUrl` a `paths` relativos, y después arreglar los dos errores. Mientras tanto el proyecto no tiene red de seguridad de tipos, que es justo lo que más se extraña al refactorizar. Detectado 2026-07-29. Ver [[frontend-architecture]].
- [ ] **`PropertyCardUI` no lleva `currency`, así que el feed asume COP.** El mapper (`usePropertyMapper`) descarta el campo al convertir `PropertyCard` → `PropertyCardUI`, y `formatCurrency` cae a su default. Hoy es correcto porque solo hay Bogotá, pero `Property` soporta cinco monedas y el card mostraría `$` sobre un precio en USD sin avisar. Se hizo visible al unificar los formateadores el 2026-07-29 — antes el `$` estaba hardcodeado en el template, con el mismo supuesto pero sin dejar rastro. Ver [[frontend-architecture]].
- [ ] **Error/loading states en FeedView y MapView.** Ambas vistas carecen de skeleton de carga y de manejo de errores visibles. Sin esto, cualquier fallo de red en demo es invisible para el usuario y difícil de diagnosticar. Ver [[frontend-architecture]].
- [ ] **`checkAuth` siempre loguea 401 en consola para usuarios no autenticados.** El catch no filtra el 401 esperado — aparece como error visual en devtools aunque el flujo es correcto. Fix: `if (axios.isAxiosError(error) && error.response?.status !== 401)` antes de loguear. Natural hacerlo junto con la centralización del axios instance. Ver `stores/auth.ts:87`. **Update 2026-06-12**: el catch de `checkAuth` ya es silencioso ([auth.ts:87-93](frontend/src/stores/auth.ts#L87-L93)) pero el 401 **sigue apareciendo** en consola — es el log nativo de red del navegador (request XHR fallido), no suprimible desde JS. Cerrarlo de verdad implica evitar el request especulativo (p. ej. solo llamar `checkAuth` si hay señal de sesión previa) o aceptar el log como ruido conocido.

- [ ] **IMPORTANTE — `@vueform/multiselect` se usa en 4 archivos pero no está declarado en `package.json`.** Llega como dependencia transitiva de `@vueform/vueform`. Cualquier cosa que limpie el árbol de dependencias (desinstalar Vueform, un `npm prune`, un lockfile regenerado con otra resolución) rompe `FeedFilters`, `LocalitySelector` y `NeighborhoodSelector` **sin ningún aviso del bundler hasta que falle el build**. El fix es de un comando (`npm i @vueform/multiselect`), y hacerlo desbloquea la pregunta de fondo: `@vueform/vueform` está registrado en `main.ts` pero ningún template usa sus componentes, y arrastra además `country-phones`, `slider` y `toggle`, ninguno usado. Detectado 2026-07-28 auditando dependencias. Ver [[frontend-architecture]].
- [ ] **Al encolar un import el admin no recibe ninguna confirmación.** `BulkUploadPropertiesModal` emite `queued` con el `batch_id`, pero `AdminPropertiesImportsView` lo monta sin listener y el proyecto no tiene sistema de toasts — el modal simplemente cierra en silencio. Es la mitad visible del hueco del endpoint de listado de jobs (ver properties-service arriba): sin toast el admin no sabe que arrancó, y sin listado no puede ir a verlo después. Lo mínimo es un toast con el `batch_id`; lo correcto es que linkee a la vista de jobs cuando exista. Ver [[frontend-admin-panel]].

- [ ] **`types/admin.ts` declara 5 monedas y el enum del backend tiene 7.** `AdminPropertyRow.currency` lista `COP|USD|EUR|MXN|PEN`; falta `CLP` y `ARS`, que `Currency` sí tiene ([listing.py:42](backend/properties-service/src/app/models/listing.py#L42)). `types/properties.ts` ya lista las siete, así que el tipo admin es el que quedó desactualizado. No rompe hoy porque no hay inventario en esas monedas, pero cuando lo haya `formatCurrency` va a recibir un valor que TypeScript juraba imposible. Es una línea. Detectado 2026-08-01. Ver [[frontend-admin-panel]].
- [ ] **La fila de la tabla admin no puede mostrar miniatura, y sin eso el panel de preview no ahorra trabajo.** El panel resuelve *decidir* sobre una propiedad, pero no *escanear*: para encontrar la sospechosa entre 20 hay que abrirlas una por una. Un thumbnail en la fila lo resolvería, pero `AdminPropertyCardSchema` no incluye `images` — `PropertyCardSchema` sí. Es agregar el campo al schema admin y la columna al front. Detectado 2026-08-01. Ver [[frontend-admin-panel]], [[properties-service-admin]].
- [ ] **La selección del panel de preview no vive en la URL.** La tab de moderación autoselecciona la primera fila y guarda `selectedId` en un `ref` local, así que un reload pierde lo que estabas mirando y no se puede compartir el link de una propiedad en revisión. Debería ser un query param, por el mismo motivo que los filtros. Detectado 2026-08-01. Ver [[adr-admin-tabs-nested-routes]], [[frontend-admin-panel]].

- [ ] **Las cuatro vistas del panel admin son el mismo esqueleto copiado, y ya van tres composables de paginación calcados.** `AdminModerationView`, `AdminPromotionsActiveView`, `AdminPromotionsCreateView` y `AdminImportsView` repiten `AdminSplitView` + tabla + `useRowSelection` + el bloque de paginación del footer; `useAdminProperties`, `useActivePromotions` y `useBulkJobs` repiten la misma paginación por offset contra distinto endpoint; y moderación e imports repiten literalmente el trío `urlFilters` / `onApplyFilters` / `watch(..., { immediate: true })` que pone los filtros en la query. La salida **no** es un factory de componentes —fabricar componentes esconde qué se renderiza— sino un composable genérico (`useAdminList<T>` con endpoint y filtros) más un contenedor con slots; el trío de filtros de URL es extraíble ya mismo tal cual, a un `useUrlFilters(definitions)`. Ojo con abstraer de más: las diferencias no son ruido —moderación acumula páginas en memoria (`usePagination`) mientras promociones y jobs piden siempre al servidor, moderación refresca el panel tras moderar (`previewPanel.refresh()`) y las otras no, y el panel de imports recibe la fila entera más un fetch de errores mientras el de properties recibe solo el id— así que una abstracción hecha antes de tiempo se llena de flags para representarlas. El momento razonable es cuando el relanzar de bulk jobs esté cerrado y las cuatro vistas dejen de moverse. Detectado 2026-08-24 al cablear la tab de importaciones, que fue la cuarta copia. Ver [[frontend-admin-panel]], [[frontend-architecture]], [[adr-admin-offset-pagination]].

### Observabilidad y telemetría

Los tres pilares están ausentes hoy. Sin los tres juntos es imposible diagnosticar degradación en producción: los logs dicen *qué* falló, las métricas dicen *cuándo* empezó, las trazas dicen *dónde exactamente*.

#### Pilar 1 — Logs
- [ ] **Logging estructurado uniforme.** Hoy cada MS configura su propio logger con distintos formatos. Unificar en JSON con campos fijos (`service`, `trace_id`, `level`, `message`) para poder agregar en una sola vista (Loki, CloudWatch Logs, etc.). El `correlation_id` middleware de properties-service es un buen punto de partida.

#### Pilar 2 — Métricas
- [ ] **Métricas de infraestructura por MS (Prometheus/Grafana).** Exponer métricas clave: requests/s, p95 latencia, errores por endpoint, hit rate del cache Redis. Sin esto no hay forma de detectar degradación antes de que el usuario la reporte.
- [ ] **Métricas del AVM.** Latencia de inferencia, tasa de errores `PREDICTION_FAILED`, distribución de precios predichos. Permite detectar drift del modelo sin auditoría manual. Ver [[analytics-service]], [[avm-training]].
- [ ] **Métricas de negocio.** Tamaño del feed devuelto, tasa de listings sin impresiones (supply invisible), CTR de promoted listings. Distintas de las métricas de infra — miden salud del producto, no del sistema.

#### Pilar 3 — Trazas
- [ ] **Trazas distribuidas (OpenTelemetry).** Un request del feed puede tocar gateway → properties-service → catalog-service sin forma de correlacionar latencias hoy. Stack: `opentelemetry-sdk` + exportador a Jaeger o Tempo. Expandir el `correlation_id` existente a W3C TraceContext para propagación automática entre MSs. Ver [[architecture]].

### analytics-service — gaps funcionales

- [ ] **Kafka worker no levantado en startup.** `runner.py` del consumer existe pero no se llama desde `main.py` — el pipeline batch (listings → predicciones) está off by default. Fix: levantar el worker en el evento de startup de FastAPI. Ver [[analytics-service-kafka-consumer]], [[analytics-service]].
- [ ] **Promotions domain sin cache invalidation efectiva.** Los UCs `create.py` y `delete.py` de promotions tienen `except Exception: pass` en las operaciones de cache — las invalidaciones no se ejecutan si Redis tiene un error transitorio. Fix: separar la lógica de invalidación del manejo de errores de cache. Ver [[properties-service-admin]].

### Bordes operativos

- [ ] **Service account de MinIO con scope restringido para MLflow.** Hoy MLflow usa las credenciales root de MinIO (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`). Crear una service account con acceso solo al bucket `mlflow-artifacts` — limita el blast radius si las credenciales se filtran. Ver [[analytics-service-mlflow]], [[adr-mlflow-minio-stack]].
- [ ] **users-service — mismatch de env var de Brevo.** El cliente lee `BREVO_API_KEY` pero el `.env.example` declara `BREVO_SMTP_KEY`; rompe el envío de emails. Ver [[users-service-email-brevo]].
- [ ] **users-service — health router sin montar.** `routes/health.py` existe pero `api_router` no lo incluye; no hay `/v1/health`. Ver [[users-service]].
- [ ] **`.env.example` incompletos** en catalog y properties (solo declaran `DATABASE_URL` y `REDIS_URL`; faltan Keycloak, `CATALOG_URL`, MinIO, TTLs). Ver [[catalog-service-local-dev]], [[properties-service-local-dev]].
- [ ] **Seeds reproducibles.** catalog se siembra manual vía bulk endpoints; properties no tiene script de seed. Definir side-container o script de seed al startup. Ver [[catalog-service-local-dev]].
- [ ] **`tests/integration/` es scaffolding vacío en los 4 servicios.** Existen las carpetas (`adapters/`, `api/`, `services/`, `performance/` con solo `__init__.py`) y un `conftest.py` con TODOs sin implementar ("Add fixtures for test_engine, test_session, mock services, TestClient with dependency overrides") — verificado en catalog-service, mismo patrón en properties/users/analytics. Hoy solo hay unit tests con `MagicMock`/`AsyncMock` sobre UoW y cache, que nunca pasan por el `Depends()` real de FastAPI. Esto dejó pasar un bug real: `resolve_isochrone_uc` no inyectaba `cache` (ver [[catalog-service-ors]]) y ningún test lo detectó porque todos construían el UC a mano con mocks ya inyectados, sin pasar por el wiring de DI. Implementación sugerida: `TestClient(app)` end-to-end con `dependency_overrides` solo para DB/cache (SQLite en memoria o testcontainers), dejando que FastAPI resuelva la cadena de `Depends()` real. Ver [[catalog-service-local-dev]], [[adr-cache-optional-layer]].

### Consistencia / wiki

- [ ] **Divergencia de patrones de worker** (proceso separado en analytics vs APScheduler in-process en users). Documentada en [[adr-apscheduler-in-process-worker]]; revisar al escalar (N réplicas → N schedulers).
- [ ] **Huérfanos del wiki.** Agregar *links* entrantes a [[catalog-service-mapbox]], [[adr-admin-division-single-level]], [[adr-geojson-upload-pattern]], [[analytics-service-testing]] (hoy solo alcanzables desde INDEX).

---

## Cerrados

- [x] **IMPORTANTE — no hay endpoint que liste los bulk jobs, así que un import es irrevisable.** **Resuelto 2026-08-24**: `GET /admin/properties/bulk` pagina las corridas por offset con `total` y filtra por estado, por si dejaron filas afuera y por rango de fechas. La fila lleva `error_count` y no los errores — el detalle sigue saliendo del status por `job_id`, que así dejó de ser un endpoint sin consumer. La tab de importaciones lo consume desde el mismo día. Ojo con el detalle de implementación: la ruta va declarada antes de `/properties/{property_id}` o FastAPI intenta parsear `bulk` como UUID y devuelve 422. Sigue abierto el flujo de retry, que necesita su propio endpoint. Ver [[properties-service-admin]], [[frontend-admin-panel]]. Texto original: **IMPORTANTE — no hay endpoint que liste los bulk jobs, así que un import es irrevisable.** Solo existe `GET /admin/properties/bulk/{job_id}/status`, que exige un `job_id` que ya haya que tener — y el modal del front descarta el `batch_id` al cerrar (por diseño: no hace polling, "el resultado se revisa en otro lado"). Hoy ese "otro lado" no existe: una vez cerrado el modal no hay forma de llegar al resultado de un import, ni a sus errores, ni de saber si terminó. Falta `GET /admin/properties/bulk` — la tabla `bulk_jobs` ya guarda todo lo que esa vista mostraría (`status`, `total_rows`, `inserted`, `errors`, timestamps, `created_by`), así que es paginar y serializar, con el mismo patrón offset de [[adr-admin-offset-pagination]]. Bonus: le daría punto de entrada al flujo de retry (`retry_of_job_id` ya existe en la tabla). Detectado 2026-07-28 al recablear el modal. Ver [[frontend-admin-panel]], [[properties-service-bulk-create-worker]].
- [x] **IMPORTANTE — la tab de moderación no tiene barra de filtros, así que no hay cola de trabajo.** **Resuelto 2026-08-24**: `AdminFilterBar` se monta en un slot `filters` nuevo de `AdminSplitView` y filtra por `verification_status` y `status`. Los filtros viven en query params, que era la mitad pendiente de [[adr-admin-tabs-nested-routes]]: el click empuja a la URL con `push` y un solo `watch` con `immediate` recarga desde ahí, así que reload, link compartido y botón atrás funcionan solos. La query se sanea contra las mismas options del enum antes de salir, de modo que una URL escrita a mano no puede provocar un 422. La página sigue fuera de la URL a propósito. Ver [[adr-admin-filters-in-query-params]], [[frontend-admin-panel]]. Texto original: **IMPORTANTE — la tab de moderación no tiene barra de filtros, así que no hay cola de trabajo.** `GET /admin/properties` acepta `status`, `verification_status` y `owner_id`, y `useAdminProperties` ya los transporta en un `ref` — lo único que falta es la UI. Sin ella el admin ve 18.744 filas sin forma de armarse una cola ni de saber qué le queda por revisar, que es justo lo que vuelve usable el panel. Al construirla, los filtros deberían vivir en query params y no en el composable, que es la mitad pendiente de [[adr-admin-tabs-nested-routes]] (la decisión de rutas anidadas se tomó precisamente para que ese estado pueda vivir en la URL). Detectado 2026-08-01. Ver [[frontend-admin-panel]].
- [x] **El pie del panel de vista previa es el formulario de moderación fijo.** **Resuelto 2026-08-09, el mismo día**: `AdminPropertyPreviewPanel` expone un slot `#footer` con la property cargada como slot prop, y perdió las props de guardado y el emit `save`. Moderación pasa su formulario, promocionar el de duración y prioridad, y activas un botón que abre el modal de confirmación. De paso el panel dejó de reenviar eventos de dos saltos (formulario → panel → vista). Ver [[frontend-admin-panel]], `docs/sources/frontend/2026-08-09-promotions-tab-wired.md`. Texto original: **El pie del panel de vista previa es el formulario de moderación fijo.** `AdminPropertyPreviewPanel` monta `AdminModerationForm` adentro, así que las dos vistas de promociones —que reusan el mismo panel— van a mostrar el formulario de moderación apenas tengan filas, con un "Guardar" que no escucha nadie. Hoy no se ve porque esas listas están vacías. El arreglo acordado es que el pie sea un slot y cada tab pase el suyo (moderación el de moderación, promociones el de prioridad/días); hay que hacerlo **antes** de cablear promociones. Detectado 2026-08-09. Ver [[frontend-admin-panel]], [[adr-promotions-own-subtab]].
- [x] **`GET /admin/promotions` sin paginar.** **Resuelto 2026-08-09** junto con el cambio de schema: devuelve `AdminPromotionsPage` (`items`/`total`/`page`/`page_size`) con offset, mismo patrón que el listado de properties ([[adr-admin-offset-pagination]]). El orden es `priority desc, ends_at asc, id` — el `id` desempata para que el offset no repita ni saltee filas cuando dos promociones comparten prioridad y fecha. De paso se le quitó la cache: leía y escribía `feed_ads_global()`, la key de ads del feed público, así que cambiarle la forma a la respuesta la habría envenenado para todos los lectores del feed. Ver [[properties-service-admin]]. Texto original: **`GET /admin/promotions` sin paginar.** `promotions/list_all.py:16-43` (repo `sql_promotion_repository.py:27-29`, `SELECT ... WHERE is_active` sin límite) — a diferencia de `GET /admin/properties`, que sí pagina con `page_size` acotado ≤100 (`admin_schemas.py:22-27`). Con volumen de promociones creciendo, respuesta de tabla completa + cache entry de `feed_ads_global` sin cota. Copiar el patrón de paginación ya existente en properties.
- [x] **`set_status.py` no invalida el cache de ads del feed.** **Resuelto 2026-08-09**: `SetPropertyStatusUseCase` suma `feed_ads_global()` y `feed_ads_by_city(city_id)` a las keys que ya borraba, y `DeletePropertyUseCase` —que tenía el mismo hueco y no estaba anotado— también. Se invalidan siempre, sin averiguar antes si la property estaba promocionada: esa consulta costaría una query por cada cambio de status, y borrar una key que se repuebla sola en el primer feed sale más barato. Con esto los cuatro caminos por los que una property deja de ser un ad válido invalidan (crear promoción, quitarla, cambiar status, borrar la property); el quinto —vencer por `ends_at`— quedó como ítem propio arriba. Ver [[properties-service-admin]], [[properties-service-search]]. Texto original: **`set_status.py` no invalida el cache de ads del feed.** `moderation/set_status.py:54-64` invalida `cache_property`, `client_properties`, `map_h3_cell`, `public_user_properties_pattern` al cambiar el status de una propiedad, pero nunca `feed_ads_global()`/`feed_ads_by_city()` (`search/helpers/feed/ads.py`, TTL 1h). Si un moderador saca de `active` una propiedad promocionada, sigue apareciendo como ad pago en el feed público hasta que expire el TTL. Mismo patrón (stale read after admin write) que la invalidación de countries en catalog-service (ver abajo).
- [x] **IMPORTANTE — `ListPromotionsByPropertyUseCase` lee/escribe la cache key del detalle público con el schema equivocado.** **Resuelto 2026-08-09 borrando el endpoint**, no arreglándolo. `GET /admin/properties/{id}/promotions` devolvía una lista de un solo elemento con la card de la property —nunca las promociones— así que respondía "¿está promocionada? sí/no", que ya se sabe por `is_promoted` o por el filtro del listado. Sin consumidores (ni front ni internos), deprecarlo habría dejado la mina armada: cualquiera llamándolo desde `/docs` envenenaba la key del detalle público. Se borró la ruta, el UC, su dependencia y `get_all_by_property_id`, que era su único caller. Si alguna vez hace falta el historial de promociones de una property, es otro endpoint: `list[PromotionSchema]` incluyendo las inactivas. Ver [[properties-service-admin]]. Texto original: **IMPORTANTE — `ListPromotionsByPropertyUseCase` lee/escribe la cache key del detalle público con el schema equivocado.** `promotions/list_by_property.py:19-51` usa `cache_property(property_id)` — la key canónica de `PropertyDetailSchema`, poblada por `GetPropertyUseCase` (público) y `GetPropertyDetailAdminUseCase` — pero la trata como `PropertyCardSchema`. Dos efectos: (1) si lee un cache warm de detail, `is_promoted` siempre da `False` (el campo no existe en ese shape) → falso `PromotionNotFoundError` (404) aunque la propiedad esté promocionada; (2) si escribe primero, deja un payload shape-card (le faltan `condition`, `verification_status`) bajo la key de detail — la próxima lectura del detalle público de esa propiedad falla `model_validate` (silenciado por `except: pass`) y cae a DB siempre, cache roto para esa propiedad hasta que algo la reescriba bien. No es solo un bug admin — degrada tráfico público. Fix acotado a un archivo: este UC no necesita `cache_property` en absoluto, puede resolver `is_promoted` directo contra la promoción activa que ya está consultando.
- [x] **Los endpoints de moderación devuelven 204 sin cuerpo, y el composable del front no sabe recargar la página actual.** **Resuelto 2026-08-09** por la vía del refetch, no del parcheo optimista: `useAdminProperties.reload()` refetchea la página actual con los filtros vigentes (`load()` seguía reservado para aplicar o cambiar filtros, porque resetea a la 1). Por debajo se agregó `usePagination.replaceCurrentPage`, que además **descarta las páginas siguientes** — al salir una fila del filtro todas se corren un lugar, así que conservarlas mostraría duplicados al avanzar — y retrocede una página si la actual queda vacía. El matiz de la fila que desaparece se resolvió solo: como la página se vuelve a pedir al servidor, la fila que ya no califica no vuelve. El panel de detalle se refresca aparte vía `defineExpose({ refresh })`, y solo si la selección no se movió. Ver [[frontend-admin-panel]], `docs/sources/frontend/2026-08-09-moderation-wiring-and-promotions-shell.md`. Texto original: **Los endpoints de moderación devuelven 204 sin cuerpo, y el composable del front no sabe recargar la página actual.** `set_property_status`, `verify_property` y `set_estimated_price` responden `204`, así que tras actuar no llega la fila actualizada. `useAdminProperties` hoy solo expone `load()` (que resetea a la página 1), `next()` y `prev()`: aprobar algo en la página 5 devolvería al admin a la 1. Falta un `reload()` que refetchee la página actual, o parcheo optimista de la fila — con el matiz de que sobre una lista filtrada la fila debería desaparecer. Ver [[frontend-admin-panel]].
- [x] **La tabla de transiciones válidas vive solo en el backend.** **Resuelto 2026-08-09** por la opción de fondo, no por el test de contrato: las tres máquinas (`VERIFICATION_TRANSITIONS`, `LISTING_STATUS_TRANSITIONS` y la del dueño, `OWNER_VISIBILITY_TRANSITIONS`) se unificaron en [status_transitions.py](backend/properties-service/src/app/services/shared/helpers/status_transitions.py), y `GET /admin/properties/{id}` publica los destinos legales del estado actual vía `AdminPropertyDetailSchema`. Son derivados, así que se calculan a la salida (`_with_transitions`) y **no se cachean**: la entrada bajo `cache_property` sigue siendo la misma que sirve al detalle público. El front borró `constants/moderationTransitions.ts` entero y el formulario recibe los destinos como prop. Se descartaron el test de contrato en CI (detecta el drift, no lo elimina) y un endpoint de metadata con las máquinas completas (no soporta reglas condicionales al estado de la property). El mismo criterio se aplicó a promociones: el filtro `is_promoted` del listado admin expone la condición del `DuplicateActivePromotionError` antes de escribir. Ver [[adr-transitions-served-by-backend]], [[properties-service-admin]], [[frontend-admin-panel]], `docs/sources/properties-service/2026-08-09-server-driven-transitions-and-promotable-filter.md`. Texto original: **La tabla de transiciones válidas vive solo en el backend.** `_ALLOWED_TRANSITIONS` existe dos veces (una en `set_status.py`, otra en `verify.py`) y no se expone por API. Cualquier UI que ofrezca botones de moderación tiene que saber qué transiciones son legales para la fila que está mostrando, así que o duplica las dos tablas en el front —con riesgo de drift silencioso— o le ofrece al admin acciones que el backend va a rechazar con `INVALID_STATUS_TRANSITION`. Opción intermedia: que `AdminPropertyCardSchema` incluya los destinos permitidos por fila, calculados en el servidor. Ver [[properties-service-admin]].
- [x] **IMPORTANTE — las 18.744 propiedades importadas están publicadas y no se pueden verificar.** **Resuelto 2026-08-01** por la vía (a): el worker escribe `verification_status=pending` ([seed_mapper.py:243](backend/properties-service/src/app/workers/helpers/mapping/seed_mapper.py#L243)), así que cada import entra encolado. El razonamiento que lo destrabó: la transición `unverified → pending` asume un **dueño** pidiendo verificación, y en un import el admin que sube el CSV ya está pidiendo que se revise el lote — nacer en `pending` no saltea el paso, reconoce que ocurrió fuera del sistema. Es legal sin tocar la state machine porque crear no es transitar: el worker construye el modelo y hace `bulk_insert`, sin pasar por `VerifyPropertyUseCase`. El "agravante" de publicar sin revisar **se resolvió por decisión explícita, no por cambio**: publicación y verificación son ejes independientes y un listing puede estar visible mientras se revisa, así que `status=active` se mantiene — con la contracara de que el aviso falta en el feed, que quedó como ítem propio arriba. También quedaron abiertos aparte el backfill de las filas viejas y la priorización de la cola. Ver [[properties-service-admin]], [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-08-01-bulk-import-pending-verification.md`. Texto original: **IMPORTANTE — las 18.744 propiedades importadas están publicadas y no se pueden verificar.** El worker fija `status=active` + `verification_status=unverified` ([seed_mapper.py:236,243](backend/properties-service/src/app/workers/helpers/mapping/seed_mapper.py#L236-L243)), y la state machine de `VerifyPropertyUseCase` solo admite `unverified → pending` como primer salto — que en el flujo normal dispara el **dueño** al pedir verificación, cosa que no pasa en un import masivo. Resultado: entraron al feed público sin revisión y a la vez quedaron fuera de la cola de moderación; ningún admin puede aprobarlas desde el panel. Las salidas posibles: (a) que el import escriba `pending`, convirtiendo cada import en una cola de moderación —lo más simple, pero con 18.744 filas la cola nace impracticable sin acciones bulk—; (b) una acción "enviar a revisión" en el panel, que mantiene el import neutro pero necesita ser masiva para servir; (c) permitir el salto directo `unverified → verified`, que abarata el caso admin pero borra la distinción entre "nadie la miró" y "está en revisión". Ojo con el agravante independiente de cuál se elija: hoy se publican sin revisar, que es una decisión de producto que nadie tomó explícitamente. Detectado 2026-07-29 al estimar los botones de moderación. Ver [[properties-service-admin]], [[properties-service-bulk-create-worker]], [[frontend-admin-panel]].
- [x] **`npm run build` está roto por 9 errores de lint pre-existentes.** **Resuelto 2026-07-29**: los 7 de formato salieron con `lint --fix` y los dos reales resultaron ser casos distintos. El de `StepImagenes.vue` era un bug de verdad — el `computed` leía y escribía `objectUrls`, así que se auto-invalidaba y podía revocar object URLs que las miniaturas seguían usando; se reemplazó por un `watch` sobre `files`. El bloque vacío de `CreatePropertyView.vue` **no** era el bug que parecía: que el `finally` avance al paso 3 pase lo que pase es deliberado, porque ese paso es una pantalla de doble uso que muestra el error o el uploader, y el botón de publicar está gateado por `!error` — así que el estado roto que se sospechaba era inalcanzable. Se arregló solo el bloque vacío. Ver [[frontend-architecture]]. Texto original: **`npm run build` está roto por 9 errores de lint pre-existentes.** `vue-cli-service build` corre ESLint y aborta: 7 son de formato auto-fixeables (`npm run lint -- --fix`), y quedan dos reales — un `vue/no-side-effects-in-computed-properties` en `StepImagenes.vue` (un `computed` que muta) y un bloque vacío. Ninguno está en archivos tocados por el trabajo del bulk import; venía roto de antes, misma forma que el suite de tests del backend. Conviene dejarlo verde antes de la migración a Vite ([[adr-vue-cli-deferred-vite-migration]]), para que un build roto durante la migración sea atribuible a la migración. Detectado 2026-07-28.
- [x] **`firebase` (^10.12.2) sigue instalado por un solo import.** **Resuelto 2026-07-28** ejecutando [[adr-firebase-removal]], 14 meses después de decidido. Al hacerlo se descubrió que el flujo estaba más muerto de lo que decía el ítem: `initializeApp` no solo estaba comentado, es que **no existe ningún `firebaseConfig` en el repo**, así que `getAuth()` tiraba `No Firebase App '[DEFAULT]' has been created` en cualquier click; y el endpoint que llamaba después, `POST /v1/auth/login/google`, **nunca existió** en users-service. Además `RegisterView` tenía su propio botón de Google sin handler. Se quitó la dep, el import y el handler; los botones y dividers quedaron **comentados** (no borrados) en ambas vistas, porque el markup sirve tal cual cuando entre el Identity Brokering de Keycloak. Texto original: **`firebase` (^10.12.2) sigue instalado por un solo import.** `LoginView.vue` importa `firebase/auth` para el login con Google; `initializeApp` está comentado en `main.ts`. [[adr-firebase-removal]] ya decidió sacarlo (Google sign-in vía Keycloak Identity Brokering), pero la decisión no se ejecutó — es la dependencia más pesada del `package.json` sirviendo a un solo call site. Ver [[frontend-architecture]].

- [x] **IMPORTANTE — falta la migración de `bulk_jobs`; el modelo y la DB están desincronizados.** **Resuelto 2026-07-28**: se generó la migración (`f9b1c0d82f43`) y se aplicó. En el camino aparecieron tres problemas del setup de alembic, todos arreglados: `env.py` importaba los modelos como `models.*` mientras ellos se importan entre sí como `app.models.*`, cargándolos dos veces bajo identidades distintas; el autogenerate proponía dropear ~40 tablas de PostGIS/tiger por estar reflejadas y ausentes de `target_metadata` (resuelto con un hook `include_object`); y `ADD COLUMN ... NOT NULL` sobre una tabla poblada necesita `server_default`, que el autogenerate omite. Texto original: **IMPORTANTE — falta la migración de `bulk_jobs`; el modelo y la DB están desincronizados.** `f3a0c4d` agregó el modelo `BulkJob` (y la columna `Property.bulk_job_id`) sin generar migración. Hay una sola migración en el servicio (`9d80aa711364`, `down_revision=None`) y no existe `create_all` en el código, así que Alembic es la única vía: contra una DB migrada la tabla `bulk_jobs` **no existe** y todo el flujo bulk revienta (encolado, worker, endpoint de status). La columna `bulk_job_id` era peor todavía — SQLAlchemy la emitía en el `SELECT` de cualquier query sobre `Property`, así que rompía feed, detalle y create, no solo el import; se removió del modelo el 2026-07-27 (ver ítem cerrado). Los tests no lo detectan porque mockean el UoW y nunca pegan a Postgres. Pendiente: generar la migración de `bulk_jobs` con `alembic revision --autogenerate`. Detectado 2026-07-27. Ver [[properties-service-bulk-create-worker]].
- [x] **`BUCKET_BULK_PROPERTIES` sin configurar y sin bucket creado.** **Resuelto 2026-07-28**: seteado y bucket creado; el import de 20k corrió contra él. Texto original: **`BUCKET_BULK_PROPERTIES` sin configurar y sin bucket creado.** El setting tiene default vacío, no está en el `.env` raíz (que sí tiene `BUCKET_PHOTOS_PROPERTIES`), y nada en el repo crea buckets de MinIO (`mc mb`/`make_bucket`). El worker lanza `StorageMisconfiguredError` al construirse si falta. `backend/properties-service/.env.example` está desactualizado: no lista ninguno de los dos buckets. Detectado 2026-07-27.
- [x] **Trazabilidad de imports: `Property.bulk_job_id` está declarada pero muerta.** **Cerrado 2026-07-27 quitando la columna**, no cableándola. Nadie la escribía y nunca llegó a una migración, así que borrarla fue un cambio de una línea sin migración asociada — y de paso arregló que SQLAlchemy la emitiera en el `SELECT` de toda query sobre `Property` contra una DB donde no existe. Razonamiento: con ids determinísticos ([[adr-bulk-idempotent-external-id]]) el set de properties que creó un import se reconstruye desde el CSV (`uuid5(namespace, external_id)` por fila), que sigue en storage referenciado por `bulk_jobs.storage_key` — o sea que la columna no era necesaria ni para el redo. Costo asumido: no se puede consultar en SQL directo qué properties salieron de qué import. Lo que seguía pendiente del diseño original (redo, TTL, content-type) se movió a su propio ítem abierto. Texto original: **Trazabilidad de imports: `Property.bulk_job_id` está declarada pero muerta.** La columna existe como FK nullable indexada (`listing.py:90`) pero ningún código la escribe, así que hoy no se puede saber qué properties salieron de qué import. Es también la pieza que faltaba para la acción de **"redo"** (soft-delete de todas las properties de un `bulk_job_id` + reproceso como job nuevo con `retry_of_job_id`) que se diseñó el 2026-07-17 y nunca se construyó. Ojo: la parte de *dedup* de ese diseño quedó **reemplazada** por [[adr-bulk-idempotent-external-id]] (idempotencia vía `uuid5(namespace, external_id)`), pero el redo sigue siendo la respuesta correcta a "deshacer un import", que es un problema distinto de "reintentar sin duplicar". Sigue pendiente del diseño original: TTL de retención del archivo en storage y validación de content-type antes de emitir la presigned URL. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-17-bulk-async-redesign-presigned-retry.md`.
- [x] **IMPORTANTE — falta un endpoint de resolución de cuentas por email (diseñarlo bulk desde el día uno).** **Resuelto 2026-07-27** dando vuelta la dirección del endpoint existente en vez de agregar uno nuevo: `POST /v1/users/resolve` ahora recibe `list[str]` de emails y devuelve `(account_id, email)` de las cuentas **activas** que matchean. Cambio de contrato breaking, seguro porque un grep confirmó que el stub del worker era su único consumidor. Se cambiaron los 5 eslabones (ruta → UC → port → adapter → repo, con `Account.email.in_(emails)`), y del lado properties `UsersClient.get_user_ids` pasó a `resolve_by_emails`. La pregunta abierta sobre emails sin match se resolvió por rechazar la fila (ver el ítem de `owner_id`). Ver [[properties-service-users]], `docs/sources/users-service/2026-07-27-resolve-accounts-by-email.md`. Texto original: **IMPORTANTE — falta un endpoint de resolución de cuentas por email (diseñarlo bulk desde el día uno).** El refactor de `owner_id` en el bulk create de properties-service (ver ítem de properties-service arriba) necesita resolver, por cada fila del CSV, el email del dueño real contra una cuenta de users-service — y `users-service` no expone hoy ningún endpoint HTTP para eso (`get_by_email` existe solo internamente, a nivel repo/reader, usado por login/reset-password/disponibilidad de email). A diferencia del caso de catalog-service (donde ya existe el endpoint single y hay que agregar el bulk aparte), acá no hay nada construido todavía — la oportunidad es diseñarlo bulk desde el arranque (`POST /v1/users/resolve` o similar, recibe una lista de emails, devuelve los `account_id` que matchean) para no repetir el mismo error de N llamadas individuales. Detectado 2026-07-16. Ver `sql_account_reader.py`, `ports/account_reader.py`, ítem de `owner_id` en properties-service arriba.
  - Update 2026-07-18 (histórico): **Update 2026-07-18 (commit `eda7114`) — construido, pero en la dirección equivocada.** Se implementó `POST /v1/users/resolve` (`AccountReaderPort.get_accounts_bulk`, `ResolveAccountsBulkUseCase`) — pero resuelve **`account_id → email`**, no `email → account_id`. Sigue sin poder usarse para lo que motivó el ítem: el CSV de bulk create trae `email`, no `account_id`. Se armó también el lado cliente en properties-service (`UsersClient`/`UsersGateway`/`UsersAdapter`, 2026-07-19) pero está desconectado del flujo real por el mismo motivo — ver [[properties-service-users]]. Falta: agregar (o cambiar) el endpoint de users-service para filtrar por `email` y devolver `account_id`. Sigue sin decidir qué pasa con un email que no matchea ninguna cuenta (¿placeholder? ¿rechazar fila?). Ver `docs/sources/catalog-service/2026-07-18-h3-precompute-and-bulk-resolve.md`, `docs/sources/properties-service/2026-07-19-bulk-create-worker-streaming-csv.md`.
- [x] **El worker no cierra el `BulkJob`.** **Resuelto 2026-07-27**: se agregó `update_status(job_id, status, errors, confirmed_at)` al port `BulkJobRepository` y a `SqlBatchRepository`, y el worker cierra vía `finalize_job` (→ `completed` + errores serializados + `confirmed_at`) o `mark_job_failed` (→ `failed`, best-effort, sin enmascarar la excepción original). Semántica decidida: **`completed` significa "la corrida terminó", no "todas las filas entraron"** — un job con 400 errores de fila sigue siendo `completed`; `failed` se reserva para una excepción que aborta la corrida. `confirmed_at` se estampa solo en el camino feliz. Detalle de implementación importante: `update_status` escribe **solo las columnas que recibe**, porque la versión ingenua (`values(status=..., errors=errors or [])`) habría borrado errores ya registrados al marcar un job como fallido. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-27-bulk-async-import-worker.md`. Texto original: **El worker no cierra el `BulkJob`.** `BulkCreatePropertiesUseCase.execute()` (worker) procesa y devuelve un `BulkCreatePropertiesResult`, pero nunca escribe de vuelta el `BulkJob` — no setea `status`, no persiste `errors`, no marca `confirmed_at`. El front que lee el job siempre ve `pending` + `errors=[]`, pase lo que pase; toda la razón de ser del modelo (status + errores para el front) queda desconectada. `BulkJobRepository` (port) solo expone `add`/`get_by_id` — falta un método de write-back (p. ej. `mark_finished(job_id, status, errors)`) y decidir la semántica de `failed` (¿`inserted == 0`? ¿solo si explota todo el batch?). `confirmed_at` está definido en el modelo y nunca se setea — semántica frente a `status` sin decidir. Detectado 2026-07-22. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-22-bulk-create-properties-refactor.md`.
- [x] **IMPORTANTE — `POST /admin/properties/bulk` es síncrono end-to-end y puede timeoutear en el front.** **Resuelto 2026-07-27** con el patrón `202 { batch_id }` + `BackgroundTasks` + `GET /admin/properties/bulk/{job_id}/status` para polling, y upload por presigned PUT directo a MinIO (el endpoint recibe la `storage_key`, no bytes). El gotcha anticipado se confirmó y se manejó: el runner abre su propia `Session(engine)` porque las dependencias `yield` se cierran antes de que corra el `BackgroundTask`. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-27-bulk-async-import-worker.md`. Texto original: **IMPORTANTE — `POST /admin/properties/bulk` es síncrono end-to-end y puede timeoutear en el front.** `BulkCreatePropertiesUseCase.execute()` llama a `catalog-service` por cada fila (`_enrich_location`, semáforo de 50 concurrentes) antes de `bulk_insert`+`commit`, todo dentro del mismo request HTTP. El front (`propertiesApi`, `timeout: 8000`) tiene 8s — un CSV de más de un puñado de filas fácilmente supera ese timeout por la latencia acumulada de red hacia catalog-service, y el cliente reporta error aunque el proceso siga corriendo (y comiteando) del lado del servidor. Detectado 2026-07-16 al construir el modal de importación admin en el front. Refactor propuesto (patrón fire-and-forget con batch, visto funcionando en un proyecto hermano): el endpoint valida+parsea sync, crea un batch `pending`, devuelve `202 { batch_id }` de inmediato, procesa (enrich + bulk_insert + commit) en background, y expone un endpoint de status para polling desde el front. Gotcha a tener en cuenta: en FastAPI ≥0.106 las dependencias `yield` (UoW/sesión) se cierran antes de que corra el `BackgroundTask` — el worker de background necesita abrir su propia sesión, no reusar la inyectada por `Depends`. Rama creada para esto: `refactor/bulk-async-processing`, sin código todavía (2026-07-17).
  - Update 2026-07-19 (histórico): **Update 2026-07-19**: la clase se movió a `workers/bulk_create_properties.py` y se reconstruyó el pipeline de lectura — ahora streamea el CSV desde MinIO en chunks (en vez de `file.read()` completo) y resuelve ubicación con una sola llamada bulk a catalog por batch de 2500 filas (`_process_batch`, reemplaza a `_enrich_location`). **No resuelve el riesgo de arriba todavía**: no hay confirmación de que la ruta HTTP invoque esto vía `BackgroundTasks` en vez de `await` inline — el nombre del módulo (`workers/`) sugiere la intención pero no está verificado. Además, `_process_batch` quedó **desconectado** del resto del pipeline (`row_to_item`/`build_models`/`bulk_insert`) — el código viejo de `_enrich_location`/`asyncio.gather` sigue en `execute()`, ahora roto (referencia variables indefinidas), no un fallback funcional. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-19-bulk-create-worker-streaming-csv.md`.
  - Diseño refinado 2026-07-17 (histórico; la parte de dedup quedó reemplazada por [[adr-bulk-idempotent-external-id]], y `bulk_job_id`/redo/TTL siguen abiertos como ítem propio): **Diseño refinado (2026-07-17, sin implementar).** Tabla `bulk_jobs` por servicio (no compartible entre servicios — DBs separadas) con `job_type` enum discriminador — en catalog-service la misma tabla sirve para `bulk_create_neighborhoods` y `bulk_enrich_neighborhood_geometries` (ver ítem de catalog-service más abajo). Shape: `id` (uuid devuelto al caller), `job_type`, `status`, `total_rows`/`processed_count`/`success_count`/`error_count`, `created_by`, timestamps, `errors` (lista JSON por fila). Precedentes reusados en vez de inventar desde cero: shape de `kc_compensation_tasks` (users-service) y `property_image_upload_batches` (properties-service). Bulk es **solo create** — no hay verbo de "bulk update"; corregir un batch es delete-by-`bulk_job_id` + create nuevo (ver `Property.bulk_job_id` FK nullable más abajo), no un merge in-place. **Dedup: decidido NO hacer hash/idempotencia automática** — se evaluó y descartó (a) hash de fila completa como key sintética (rompe con cualquier edición legítima, en particular precio) y (b) excluir precio del hash + del `DO UPDATE` con heurística de detección de abuso a nivel de batch (juzgado sobre-ingeniería para la necesidad real). Reintentar con el mismo archivo sin usar la acción de redo puede duplicar — comportamiento esperado y documentado (tooltip en el front), no prevenido por el sistema. Mitigación real: `Property.bulk_job_id` (FK nullable) para trazabilidad + acción de "redo" que soft-borra (mismo patrón que `delete_property.py`, no hard delete) todas las properties de ese `bulk_job_id` y reprocesa como job nuevo con `retry_of_job_id` apuntando al original (el job fallido queda en el historial, no se pisa). El delete filtra server-side por `bulk_job_id`, no recibe lista de ids del front. **Simplificación grande**: el CSV se sube por presigned URL directo a MinIO (mismo patrón que `property_image_upload_batches` para fotos) en vez de proxyarse por el request (hoy `file.read()` sync en `admin.py`) — el endpoint bulk recibe la storage key, no bytes. Consecuencia: un reintento no resube nada, el background job relee el mismo objeto de storage — cero bandwidth en el reintento. Pendiente: TTL de retención del archivo en storage (para que un reintento no reuse una key vieja con datos stale indefinidamente), validación de content-type antes de emitir la presigned URL, expiración de la URL presigned misma. Ver [[adr-image-upload-presigned-batch]] (patrón de referencia), `docs/sources/properties-service/2026-07-17-bulk-async-redesign-presigned-retry.md`.
- [x] **IMPORTANTE — bulk create le pone `owner_id` = el admin que ejecuta el import, no el dueño real.** **Resuelto 2026-07-27**: el CSV trae columna `email` por fila y el worker resuelve `email → account_id` contra users-service, poblando `Property.owner_id` con la cuenta real; `created_by` sigue siendo el admin. La pregunta que quedaba abierta ("¿qué pasa si no matchea ninguna cuenta?") se resolvió por **rechazar la fila**: el email sin cuenta activa no vuelve en la respuesta, la fila se reporta como `"owner not resolved for email"` con su línea y queda en `bulk_jobs.errors` — nunca se asigna a un dueño equivocado. Ver [[properties-service-users]], `docs/sources/properties-service/2026-07-27-bulk-async-import-worker.md`. Texto original: **IMPORTANTE — bulk create le pone `owner_id` = el admin que ejecuta el import, no el dueño real.** `build_models(item=item, owner_id=principal.sub, created_by=principal.sub)` usa el mismo UUID (el admin logueado) para ambos campos — hoy todas las propiedades importadas en bloque quedan "de propiedad" del admin, apareciendo en su propio `GET /properties/me`. `created_by=principal.sub` está bien (es auditoría real: quién ejecutó la acción). `owner_id` está mal: debería resolverse contra una cuenta real del dueño, no hardcodearse al importador. Detectado 2026-07-16, mismo hallazgo que el ítem de arriba (mismo endpoint). Decisión tomada: resolver `owner_id` por **email** — ya existe como campo único+indexado en `Account` (users-service), sin necesitar migración ni nuevo dato de identidad (se descartó cédula: no existe ese campo en el sistema hoy, agregarlo implica migración + decidir qué hacer si no matchea). El CSV de bulk necesitaría una columna de email por fila; si no matchea ninguna cuenta, a definir (¿crear cuenta placeholder? ¿rechazar la fila?) — email es solo el punto de partida, en onboarding se podrían pedir más datos de identidad más adelante. Bonus: desbloquea poder atribuir imports de demo a varias cuentas fabricadas en vez de que todo quede bajo un mega-dueño (un solo admin con cientos de propiedades no se ve realista en un demo). Trazabilidad del batch: no hace falta un mecanismo aparte — la entidad de batch que ya se necesita para el refactor async de arriba (`202 + batch_id`) puede servir también para rastrear qué `property_id`s salieron de qué import. Ver `seed_mapper.py` (`build_models`), `bulk_create_properties.py`, `Account` en users-service (`models/account.py` — solo `account_id`/`email` como identificadores únicos hoy).
- [x] **Errores de bulk create sin identificador de row.** Resuelto 2026-07-22 con el schema estructurado `BulkRowError(line, ref, issues)`: `line` es un contador monotónico sobre el generador (número de fila real, no el índice windowed del batch), `ref` = `email @ lat,lon` de la fila cruda (helper `row_ref`), `issues` = detalle de pydantic (`e.errors()`) o el motivo del fallo de geo-resolución. Reemplaza el `list[str]` sin referencia al row. Ver [[properties-service-bulk-create-worker]], `docs/sources/properties-service/2026-07-22-bulk-create-properties-refactor.md`.
- [x] **Contradicción PostGIS "único servicio"** — corregida en catalog (overview, runbook, ADR) y glossary; properties-ms-db también usa `postgis/postgis:17-master` (2026-05-28).
- [x] **Contradicción auth Bearer vs cookie** — corregida en [[analytics-service-architecture]] y [[architecture]]; todos los servicios leen el JWT de la cookie `access_token` (2026-05-28).
- [x] **Conectar `gmp-placeselect` al chain completo** en `DevPlaygroundView.vue` (place → coords → catalog by-coords → `/v1/predict`). Cableado 2026-05-29 — ver [[frontend-architecture]], [[adr-gmaps-places-geocoding]]. **Nota (2026-07-13)**: esa lógica ya no vive en `DevPlaygroundView.vue` — se movió a `StepUbicacion.vue` (flujo de creación de propiedad); el evento correcto además es `gmp-select`, no `gmp-placeselect` (ver ADR-0005 actualizado).
- [x] **Vista contenedora feed/mapa con toggle.** View que orquesta nested routes y alterna entre la subview de **feed** (cards) y la de **mapa** — la view padre mantiene `FeedFilters` y header compartidos alrededor del slot. Decisión tomada: nested route (URL bookmarkeable, no `v-if`). Ver [[frontend-architecture]], [[properties-service-search]]. Implementado 2026-06-08.
- [x] **MapView con Leaflet + bbox + paginación.** `/feed/map` con `GetFeedMapUseCase`: bbox del viewport → celdas H3 → markers en Leaflet, paginación local por flechas, hover state, URL state vía `router.replace`. Implementado 2026-06-09 — ver [[frontend-architecture]], [[frontend-map-component]].
- [x] **`BoundingBox.to_polygon()` instancia una clase abstracta de h3.** Fix: `h3.LatLngPoly(...)` y return type actualizado. Corregido 2026-06-09 — ver [[properties-service-search]].
- [x] **H3 pre-filter antes de `ST_Within` en `GetFeedMapUseCase`.** El repo ya filtra **solo por columna H3** (`h3_r7.in_(cells)` o `h3_r9.in_(cells)` según la resolución) — no hay `ST_Within` en este path. La precisión del bbox es la de las celdas (`contain="center"`). Verificado 2026-06-09 — ver [[properties-service-search]], [[adr-postgis-h3-hybrid]].
- [x] **Refactor sección POI de `PropertyDetailView`.** La sección entera se extrajo a `NearbyPlaces.vue` (autocontenido, dueño de `useReachablePois`); `CATEGORY_TO_MARKER` se eliminó (reemplazado por `CategoryMeta.bucket` en `types/pois.ts`), sin imports muertos. Cerrado 2026-06-20 — ver [[frontend-poi-reachable]], [[frontend-map-component]].
- [x] **Cache-aside para `reachable-pois` (duplicado).** Era el mismo ítem que el de la sección "Infra compartida" (key por `property_id` o por h3 cell, TTL 1h) — duplicado detectado por `/wiki-lint`, consolidado ahí. Implementado 2026-06-20 — ver [[catalog-service-ors]], [[adr-isochrone-ors-h3]].
- [x] **Endpoint de resolución por coordenadas en catalog para el front.** `GET /v1/geo-resolution/by-coordinates` (`ResolveLocationByCoordinatesUseCase`) cubre lo que el ADR [[adr-gmaps-places-geocoding]] pedía — ya wired end-to-end desde el frontend. Verificado 2026-07-13 vía `/wiki-lint`.

## Claims

- El directorio `workers/` de properties-service contiene el bulk-create worker y sus helpers, pero ningún consumer Kafka ([workers/](backend/properties-service/src/app/workers)).
- El path ML de `SetEstimatedPriceUseCase` (principal=None) no tiene caller al 2026-05-28 ([set_estimated_price.py:26-32](backend/properties-service/src/app/services/admin/use_cases/estimated_price/set_estimated_price.py#L26-L32)).
- El cliente Brevo lee `BREVO_API_KEY`, pero el `.env.example` de users-service declara `BREVO_SMTP_KEY` ([client.py:15](backend/users-service/src/app/integrations/email/brevo/client.py#L15), [backend/users-service/.env.example](backend/users-service/.env.example)).
- El `api_router` de users-service no incluye el health router ([api/main.py:3-8](backend/users-service/src/app/api/main.py#L3-L8)).
- Los `.env.example` de catalog y properties solo declaran `DATABASE_URL` y `REDIS_URL` ([backend/catalog-service/.env.example](backend/catalog-service/.env.example), [backend/properties-service/.env.example](backend/properties-service/.env.example)).
- El flujo async properties↔analytics figura como "en definición" en la arquitectura cross-service ([architecture.md](docs/wiki/_shared/architecture.md)).
- `BoundingBox.to_polygon()` fue corregido a `h3.LatLngPoly(...)` — corregido 2026-06-09 ([feed_schemas.py](backend/properties-service/src/app/services/search/schemas/feed_schemas.py)).
- `PropertyDetailView.vue` + `usePropertyDetail.ts` existen en la ruta `/listing/:id` — la base de la vista detalle ya está implementada al 2026-06-12 ([frontend/src/views/](frontend/src/views/)).
- El stack Redis (`integrations/cache/redis/` + adapter + port + exceptions) está triplicado en catalog, properties y users — ningún hash de archivo coincide entre copias (verificado 2026-06-12).
- El stack MinIO (`integrations/storage/minio/` + adapter + port + exceptions) está duplicado en properties y users con implementaciones funcionalmente divergentes: properties usa presigned PUT URLs, users usa `upload_file` server-side (verificado 2026-06-12).
